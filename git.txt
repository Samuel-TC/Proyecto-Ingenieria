git init 
git status / para verificar lo que se va a subir
git add  . /
git commit -m "mensaje" / confirmar el cambio 
git config --global user.email "you@example.com"
git config --global user.name "Your Name"
git branch -M main
